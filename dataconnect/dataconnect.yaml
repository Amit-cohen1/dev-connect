specVersion: "v1beta"
serviceId: "dev-connect"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "dev-connect-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
